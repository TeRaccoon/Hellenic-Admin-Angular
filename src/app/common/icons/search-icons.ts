import {
  faX,
  faSearch,
  faSpinner,
  faFileCircleXmark,
} from '@fortawesome/free-solid-svg-icons';

export const searchIcons = {
  x: faX,
  search: faSearch,
  spinner: faSpinner,
  noFile: faFileCircleXmark,
};
