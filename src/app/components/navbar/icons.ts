import {
  faBell,
  faEnvelope,
  faFileCircleXmark,
  faKey,
  faRightFromBracket,
  faSearch,
  faSpinner,
  faUser,
  faX,
} from '@fortawesome/free-solid-svg-icons';

export const ICONS = {
  bell: faBell,
  envelope: faEnvelope,
  user: faUser,
  search: faSearch,
  noResults: faFileCircleXmark,
  loading: faSpinner,
  key: faKey,
  exit: faRightFromBracket,
  x: faX,
};
